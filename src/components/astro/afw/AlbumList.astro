---
interface Props {
  albums?: string[];
}

const { albums } = Astro.props;
---

<div class="my-4">Featured Albums:</div>
<ul class="list-disc ml-4">
  {albums?.map((album) => <li class="font-medium">{album}</li>)}
</ul>
